<?xml version="1.0" encoding="utf-8" ?>
<ContentPage Title="Login"
             BackgroundColor="#127ac7"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="InternChatRoom.LoginPage">
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0" VerticalOptions="Center">
            <Label Text="Welcome" FontSize="40" TextColor="White" HorizontalOptions="Center"/>
            <Label Text="to the Secret Chat Room" FontSize="25" TextColor="White" HorizontalOptions="Center"/>
            <Label Text="Login" FontSize="30" TextColor="White" HorizontalOptions="Center"/>
            <Entry Text=""  Placeholder="Username"/>
            <Entry IsPassword="True" Placeholder="Password"/>
        </StackLayout>
        <Button Grid.Row="1" Text="Login" BackgroundColor="#1dabf0" TextColor="White" Clicked="Button_Clicked"/>
        <Button Grid.Row="2" Text="Register" BackgroundColor="#1dabf0" TextColor="White" Clicked="Button_Clicked_1"/>
    </Grid>
</ContentPage>